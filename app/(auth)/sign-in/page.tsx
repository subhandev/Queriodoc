import { SignInForm } from "@/components/auth/SignInForm";

export const metadata = {
  title: "Sign in — Queriodoc",
  description: "Sign in to Queriodoc to keep chatting with your documents.",
};

export default function SignInPage() {
  return <SignInForm />;
}
